[phases.setup]
nixPkgs = ["nodejs_22", "poppler_utils"]

[phases.install]
cmds = ["npm install --legacy-peer-deps --include=optional", "npm install --os=linux --cpu=x64 sharp"]

[start]
cmd = "npm start"
